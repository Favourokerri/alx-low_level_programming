#include "main.h"

/**
 * _strlen - length of a string
 * @s: string to be checked
 * Return: 0 (success)
 */

int _strlen(char *s)
{
	int i;

	s[] = i;

	for (s[i] = 0; s[i] != '\0'; s[i]++)
	return (0);
}
