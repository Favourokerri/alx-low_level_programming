#include <stdio.h>
/**
 * main - A programm that prints in reverse
 * Return: 0 (success)
 */
int main(void)
{
	char letter;

	letter = 'z';
	while
		(letter >= 'a') {
		       putchar(letter);
		       letter--;
			}
	putchar('\n');
	return (0);
}
